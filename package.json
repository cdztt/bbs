{
  "scripts": {
    "devhttp": "cross-env NODE_ENV=dev NODE_PROTOCOL=http nodemon -w ../WS -e js,html ./index.js http",
    "devhttps": "cross-env NODE_ENV=dev NODE_PROTOCOL=https nodemon -w ../WS -e js,html ./index.js https",
    "starthttp": "cross-env NODE_ENV=prod nodemon --openssl-legacy-provider ./index.js http",
    "starthttps": "cross-env NODE_ENV=prod nodemon --openssl-legacy-provider ./index.js https"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}